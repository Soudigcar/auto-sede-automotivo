name: Lock sold catalog vehicles

on:
  push:
    branches:
      - refactor/marketplace-catalog

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: refactor/marketplace-catalog
          fetch-depth: 0

      - name: Lock sold vehicles and remove temporary files
        run: node scripts/lock-sold-catalog-vehicles.mjs

      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: block sold vehicle edits in catalog"
          git push origin HEAD:refactor/marketplace-catalog
